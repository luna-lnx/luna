# this package is an example. don't actually use it

name = "example"
description = "an example package. nothing of interest."
tag = "v1.2.4"
tarball = "https://git.musl-libc.org/cgit/musl/snapshot/musl-1.2.4.tar.gz"
# TODO: add other formats
dependencies = [
    "make"
]
build = [
    "./configure",
    "make"
]
install = [
    "make install"
]
uninstall = [
    "make uninstall"
]